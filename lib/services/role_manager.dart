/// Service for managing the current user role within the application.
/// 
/// Dictates permissions and available views (e.g., 'manager', 'driver').
class RoleManager {
  /// The currently active role string.
  static String currentRole = 'manager';
}
